!!! 5
%html.no-js
  = render 'head'

  %body{ :class => "#{ controller.controller_name }" }
    = render 'chromeframe'
    = render 'header_onboarding'
    .page-wrapper
      .tips-header
        = yield :step_info
      = render 'flashes'

      = yield

      = render 'dialog'

      = render 'footer'

    -# Javascript at the bottom for fast page loading
    = render 'javascripts'
