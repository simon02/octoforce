!!! 5
%html.no-js
  = render 'head'

  %body{ :class => "#{ controller.controller_name }" }
    = render 'chromeframe'
    = render 'header'
    .page-wrapper
      = render 'flashes'

      = yield

      = render 'dialog'

      = render 'footer'

    -# Javascript at the bottom for fast page loading
    = render 'javascripts'
