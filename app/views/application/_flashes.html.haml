- if !flash.empty?
  .container#flash
    - flash.each do |key, value|
      .alert{role: 'alert', class: "#{flash_class(key)}"}
        %button.close{'data-dismiss': 'alert', type: 'button'}
          %span{'aria-hidden': 'true'}
            ×
          %span.sr-only
            Close
        = value
